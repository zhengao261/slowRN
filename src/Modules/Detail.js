'use strict';
var React = require('React');
import {
  ScrollView,
  Text
} from 'react-native';

const Detail = React.createClass({
  render() {
    return (
      <ScrollView>
        <Text>XiangQing</Text>
      </ScrollView>
    )
  }
});

module.exports = Detail;
